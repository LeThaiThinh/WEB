extends ../index
include ../mixins/searchAccountForm
block append topbar
    +topbarAdmin
block append sidebar-text
    .edit-text
        p THIS IS OUR LOGIN PAGE!
        br
        p
        | I&apos;m a paragraph. Click here to add your own text and edit me. It&rsquo;s easy. Just click &ldquo;Edit Text&rdquo; or double click me to add your own content and make changes to the font.    
    .menu-admin
      a Search Account   
      +searchAccountForm(search)
block append content
  .wrap-content
    .container-xl
      .table-responsive
        .table-wrapper
          .table-title
            .row
              .col-sm-5
                h2
                  | Account 
                  b Management
              
          table.table.table-striped.table-hover
            thead
              tr
                th #
                th Name
                th(style="min-width:120px") Birthday &nbsp;&nbsp;&nbsp;
                th Phone
                //- th Status
                th Action
            tbody
              each user in users
                tr
                  td=user.id
                    //- a 1
                  td=user.username
                  td=user.dateOfBirth
                  td=user.phone
                  //- td  
                  //-   span.status.text-success &bull;
                  //-   |  Active
                  td
                    a.settings(href=`/admin/account/${user.id}` title='Settings' data-toggle='tooltip')
                      <i class="fas fa-edit"></i>

                    //- form(action=`${user.id}/remove`, method="post")
                    //-   .formInput
                    //-     button.delete(type="submit" data-toggle='tooltip' title='Delete')  
                    //-       i.material-icons &#xE5C9;
                      
              
